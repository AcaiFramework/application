// Packages
import config from "config/mod.ts";

// -------------------------------------------------
// Global middlewares
// -------------------------------------------------

config.setConfig("global", []);

// -------------------------------------------------
// Aliased middlewares
// -------------------------------------------------

config.setConfig("middleware", {});