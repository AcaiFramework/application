// Packages
import config from "@acai/config";

// -------------------------------------------------
// List of providers
// -------------------------------------------------

config.setConfig("paths", {
	"controllers"	: "app/http/controllers",
	"views"			: "app/views",
	"routes"		: "routes",
	"models"		: "app/models",
});
