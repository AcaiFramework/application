// Packages
import config from "@acai/config";

// -------------------------------------------------
// List of providers
// -------------------------------------------------

config.setConfig("paths", {
	"controllers"	: "app/controllers/http",
	"views"			: "app/views",
	"routes"		: "routes",
	"models"		: "app/models",
});
